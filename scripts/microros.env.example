# このファイルを "<repo>/.microros.env"（推奨）または "scripts/microros.env" にコピーして、
# 自分の環境に合わせて値を編集してください。
#
# 任意の場所のファイルを使う場合は、次のように指定できます：
#   MICROROS_ENV_FILE=/path/to/file ./scripts/build_microros_lib.sh

# micro-ROSライブラリを生成するROS 2ディストロ（例: humble, jazzy）
ROS_DISTRO=humble

# micro_ros_setup をビルドするROS 2ワークスペースのルート（"src/" と "install/" があるディレクトリ）
# 例：
MICROROS_WS=/home/aa/ros2_ws
# MICROROS_WS=${HOME}/ros2_ws

# 任意の上書き：
# OUT_DIR=/path/to/this_repo/microros
# BUILD_DIR=/path/to/this_repo/.microros_build
# TOOLCHAIN_FILE=/path/to/this_repo/cmake/microros_toolchain_stm32f446.cmake
# COLCON_META=/path/to/this_repo/cmake/microros_colcon.meta
# CLEAN=1
